header.header ----------- header --------------
    nav
        a(href="/") Home
        ul
            li
                if user
                    button#logout Logout
                else
                    a(href='/login') Login
            li
                if user
                    a(href=`/${user.name}`)
                        img(src=`/img/users/${user.photo}` width="50")
                        span=`${user.name}`
                else
                    a(href='/join') Signup
    .body ----------- body --------------